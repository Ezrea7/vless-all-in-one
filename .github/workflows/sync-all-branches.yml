name: Sync vless-server.sh

on:
  schedule:
    - cron: '0 */3 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Download and sync
        run: |
          # 下载最新文件
          curl -fsSL https://raw.githubusercontent.com/Chil30/vless-all-in-one/main/vless-server.sh -o vless-server.sh.new
          
          # 检查是否有变化
          if diff -q vless-server.sh vless-server.sh.new > /dev/null 2>&1; then
            echo "✅ 文件无更新"
            rm vless-server.sh.new
            exit 0
          fi
          
          # 有更新，替换文件
          echo "🔄 检测到更新"
          mv vless-server.sh.new vless-server.sh
          chmod +x vless-server.sh
          
          # 提交推送
          git add vless-server.sh
          git commit -m "🔄 sync: update vless-server.sh ($(date -u '+%Y-%m-%d'))"
          git push
          
          echo "✅ 同步完成"